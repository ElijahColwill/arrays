def haveThree(a):
    threes = 0
    for num in a:
        if num == 3:
            threes += 1
    return threes == 3

def sum28(b):
    sum = 0
    for num in b
        if num == 2:
            sum += 1
    return sum == 4

# Given an array of ints, return true if the sum of all the 2's in the array is exactly 8.
